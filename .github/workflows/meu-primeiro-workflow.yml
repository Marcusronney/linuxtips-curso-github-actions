name:  Primeiro Workflow Linuxtips

on:
  workflow_dispatch

jobs:
  giropops:
    runs-on: ubuntu-latest
    steps:
      - name: Confere Versao do ubuntu
        run: cat /etc/os-release
      
      - name: Exibir uma mensagem
        run: echo "estou fazendo o curso de Github Actions na LinuxTips"